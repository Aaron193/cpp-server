import { FastifyPluginAsync } from 'fastify'

const usersRoutes: FastifyPluginAsync = async () => {
    // Placeholder for future user-related routes
    // e.g., profile updates, password changes, etc.
}

export default usersRoutes
